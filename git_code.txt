
#One-liner to stage ALL files (modified, deleted, and new) and commit with comment:
git add --all && git commit -m "comment"
----------------------------------------------------

# git clean -d -i
delete file interaction


------------------------------------------------------------
# git ignore

https://www.youtube.com/watch?v=ErJyWO8TGoM


#Adding an existing project to GitHub using the command line

https://docs.github.com/cn/github/importing-your-projects-to-github/importing-source-code-to-github/adding-an-existing-project-to-github-using-the-command-line

打开 Git Bash。
Change the current working directory to your local project.
Initialize the local directory as a Git repository.
$ git init -b main

Add the files in your new local repository. This stages them for the first commit.
$ git add .

# Adds the files in the local repository and stages them for commit. 要取消暂存文件，请使用 'git reset HEAD YOUR-FILE'。
Commit the files that you've staged in your local repository.
$ git commit -m "First commit"

# Commits the tracked changes and prepares them to be pushed to a remote repository. 要删除此提交并修改文件，请使用 'git reset --soft HEAD~1' 并再次提交和添加文件。

At the top of your repository on GitHub.com's Quick Setup page, click  to copy the remote repository URL.
Copy remote repository URL field

In the Command prompt, add the URL for the remote repository where your local repository will be pushed.
$ git remote add origin  <REMOTE_URL> 

# Sets the new remote
$ git remote -v

# Verifies the new remote URL
Push the changes in your local repository to GitHub.com.
$ git push origin main

# Pushes the changes in your local repository up to the remote repository you specified as 

$ git init -b main
warning: re-init: ignored --initial-branch=main
Reinitialized existing Git repository in C:/Users/adele/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code/.git/
(PythonData)
adele@White MINGW64 ~/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code (main)
$ git add .
(PythonData)
adele@White MINGW64 ~/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code (main)
$ git commit -m "First commit"
On branch main
nothing to commit, working tree clean
(PythonData)
adele@White MINGW64 ~/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code (main)
$ git remote add origin https://github.com/MeinaBian/CU_Btcamp_Challenge3.git
(PythonData)
adele@White MINGW64 ~/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code (main)
$ git remote -v
origin  https://github.com/MeinaBian/CU_Btcamp_Challenge3.git (fetch)
origin  https://github.com/MeinaBian/CU_Btcamp_Challenge3.git (push)
(PythonData)

$ git push --set-upstream origin main
Everything up-to-date
Branch 'main' set up to track remote branch 'main' from 'origin'.
(PythonData)

adele@White MINGW64 ~/Desktop/CU-VIRT-FIN-PT-12-2021-U-B/Starter_Code (main)
$ git push
-----------------------------------------------


# git_cheatsheet_nextline
Interpreting newlines as <br /> used to be a feature of Github-flavored markdown
or 
a
b
c
into

a__
b__
c
(where _ is a blank space).

Or, you can add explicit <br /> tags.

a <br />
b <br />
c


